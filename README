# Simple experimental RMK config for Saurus keyboard.

With Nix:
  nix develop
  cargo make uf2 --release

Install tools:
  cargo install cargo-make flip-link

Then build with this command:
  cargo make uf2 --release
